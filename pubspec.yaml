name: inspectable
description: Inspectable visualizes the widget tree on running devices for debugging or research purpose.
version: 0.1.1
homepage: https://github.com/chooyan-eng/inspectable

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: